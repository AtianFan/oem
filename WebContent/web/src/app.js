define(function(require){
	"use stricts";
	var WindowManager = require("framework/WindowManager");
	/*Zqy.utils.ajax({
		url:"/VideoTradePlat/SysUser/login",
		type:'post'
	}).done(function(resp){
		console.log(resp);
	});*/
	WindowManager.pushPage("LoginPage");
});